{
  "name": "phonebook_be",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node --watch index.js",
    "build:ui": "rm -rf dist && cd ../phonebook_fr && npm run build && cp -r dist ../phonebook_be",
    "deploy:full": "npm run build:ui && cd .. && git add . && git commit -m 'uibuild' && git push"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
